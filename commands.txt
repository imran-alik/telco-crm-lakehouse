# Copy-Paste Command Log — Telco CRM Lakehouse

Run these commands inside your terminal from the project's root folder (`C:\Users\imran\Documents\Projects\telco-crm-lakehouse`) to execute, test, and verify the platform.

---

## ⚡ 1. Virtual Environment & Dependencies Setup
```bash
# Set up Python virtual environment
python -m venv .venv

# Activate virtual environment (Linux/macOS)
source .venv/bin/activate

# Activate virtual environment (Windows PowerShell)
.venv\Scripts\Activate.ps1
```

```bash
# Install core runtime and developer dependencies
pip install -r codebase/requirements.txt
```

---

## 🚀 2. Pipeline Execution
```bash
# Set PYTHONPATH environment variable (Linux/macOS)
export PYTHONPATH="codebase"

# Set PYTHONPATH environment variable (Windows PowerShell)
$env:PYTHONPATH="codebase"
```

```bash
# Step A: Generate synthetic reference source CSVs
python codebase/scripts/generate_samples.py

# Step B: Run the Medallion pipeline (Ingests all layers and builds Marts)
python codebase/scripts/run_pipeline.py --layer all

# Step C: Execute Quality-Gate & Replay Idempotency Certification
python codebase/scripts/certify_public_run.py
```

---

## 🧪 3. Verification & Test Suite Execution
```bash
# Run static style linting using Ruff
ruff check codebase tests

# Run behavioural unit, integration, and certification tests
python -m pytest tests/ -v
```

---

## 📊 4. Database Spot-Checks (DuckDB Query Runner)
Run raw SQL queries against your local database instance:
```bash
# Open database file directly using Python query runner
python -c "import duckdb; conn=duckdb.connect('data/sink/lakehouse.db', read_only=True); print(conn.execute('SHOW TABLES').fetchdf())"

# Print customer record counts with active demographics
python -c "import duckdb; conn=duckdb.connect('data/sink/lakehouse.db', read_only=True); print(conn.execute('SELECT region, plan_tier, COUNT(*) FROM gold_dim_customer GROUP BY 1, 2 ORDER BY 3 DESC LIMIT 5').fetchdf())"

# Print legal compliance mart to inspect cryptographically masked phone IDs
python -c "import duckdb; conn=duckdb.connect('data/sink/lakehouse.db', read_only=True); print(conn.execute('SELECT call_id, ani_hash, email_domain FROM mart_legal_compliance LIMIT 3').fetchdf())"
```
